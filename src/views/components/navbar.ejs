<nav class="sticky top-0 z-30 border-b border-slate-200 bg-white/90 backdrop-blur">
    <div class="container mx-auto max-w-6xl flex justify-between items-center gap-6 px-4 py-4">
        <div class="flex items-center gap-6">
            <a href="/" class="flex items-center gap-3">
                <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-900 text-white text-lg font-semibold">N</span>
                <span class="text-xl font-semibold text-slate-900">NewS</span>
            </a>
            <ul class="hidden md:flex items-center gap-3 text-sm font-medium text-slate-700">
                <li><a class="rounded-lg px-3 py-2 hover:bg-slate-100" href="/news">Новости</a></li>
                <% if (user && isAdmin) { %>
                    <li><a class="rounded-lg px-3 py-2 hover:bg-slate-100" href="/categories">Категории</a></li>
                    <li><a class="rounded-lg px-3 py-2 hover:bg-slate-100" href="/categories/create">Создать категорию</a></li>
                <% } %>
                <% if (user && (isEditor || isAdmin)) { %>
                    <li><a class="rounded-lg px-3 py-2 hover:bg-slate-100" href="/news/create">Создать новость</a></li>
                <% } %>
            </ul>
        </div>
        <% if(user) { %>
            <div class="flex items-center gap-3">
                <span class="hidden text-sm text-slate-600 md:inline">Привет, <%= user.username %></span>
                <a href="/user/profile" class="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-900 hover:bg-slate-100">Профиль</a>
                <form action="/auth/logout" method="post">
                    <button type="submit" class="rounded-lg bg-slate-900 px-3 py-2 text-sm font-semibold text-white hover:bg-slate-800 cursor-pointer">Выйти</button>
                </form>
            </div>
        <% } else { %>
            <div class="flex gap-3">
                <a href="/auth/signup" class="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-900 hover:bg-slate-100">Регистрация</a>
                <a href="/auth/login" class="rounded-lg bg-slate-900 px-3 py-2 text-sm font-semibold text-white hover:bg-slate-800">Войти</a>
            </div>
        <% } %>
    </div>
</nav>
